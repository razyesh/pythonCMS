# pythonCMS

Install the necessary package

pip install -r requirements.txt

now clone the repository and open up the terminal and run
python -m http.server --cgi

open the browser and go to the url
localhost:8000/admin

email: abc@gmail.com
pass:  kapan123
